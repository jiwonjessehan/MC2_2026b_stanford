%% LaTeX template for the science justification & technical
%% feasibility for KIPAC Magellan profiles.
%%
%% Limit 2 pages of text plus an additional page for figures and tables
%% Minimum 12 point font on letter paper with 1 inch margins.
%% Do not change the font, page or margin settings in this template!
%%
%% Edit this template and then produce a PDF file:
%%
%% pdflatex sjtf_template_new
%%
%% If you do not have pdflatex available (pre-2005 TeX installations),
%% then
%% latex sjtf_template_new
%% dvipdf sjtf_template_new
%%
%% or, if dvipdf is not available:
%%
%% latex sjtf_template_new
%% dvips sjtf_template_new
%% ps2pdf sjtf_template_new.ps
%%
%% where ps2pdf is available with ghostscript.
%%

%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%%% Document format %%%%%
%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%
%% DO NOT CHANGE THE FONT SIZE
%%
\documentclass[12pt]{article}

%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%%% Page dimensions %%%%%
%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%
%% Minimum margin size is 1 inch from top, bottom, and sides.
%% DO NOT CHANGE THE FOLLOWING LINE, IT IMPLEMENTS THE
%% MANDATORY LIMITS.
%% The science justification is strictly limited to 2 pages plus an additional page of figures, 12-point font on letter paper with 1-inch margins. 
\usepackage[letterpaper,left=1in,right=1in,top=1in,bottom=1in,headheight=0pt]{geometry}
\usepackage{amsmath}
\usepackage{wrapfig}
%%
%% Some very old LaTeX setups change these parameters. If your proposal
%% text does not appear to be centered on letter page paper, modify
%% these values appropriately.
%%
\setlength{\hoffset}{0in}
\setlength{\voffset}{0in}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%%% Included packages %%%%%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%
%% Switch to Times Roman fonts w/compatible math.
%% (Comment at will.)
%%
\usepackage{txfonts}
%%
%% Font spacing - gives better readability; this only works with
%% pdflatex, xelatex and other modern LaTeX installations, it does not
%% work with older (pre-2005) installations. If you get error
%% messages, comment the next line.
%%
\usepackage{microtype}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%%% How to include figures %%%%%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%
%% With the graphicx package, in a normal latex distribution, can be
%% included using the \includegraphics command. The graphicx package
%% is part of virtually all post-2005 LaTex distributions. If it is
%% not available, use one of the older ways of including graphics in
%% LaTeX, such as the graphics package. In the graphicx package, a
%% figure is included with
%% 
%% \begin{figure}
%% \includegraphics[key=val,key=val,...]{file}
%% \end{figure}
%%
%% where some of the useful keys are: angle, width, height,
%% keepaspectratio (='true' or 'false') and scale. Bounding box values
%% can be given as [bb=llx lly urx ury].
%% where ll denotes 'lower left' and ur 'upper right' and the x and y
%% values are the coordinates of the image bounding box in
%% points. There are 72 points in an inch.
%%
%% It is often better to ``trim'' white space away from the figure
%% rather than specifying the bounding box -- the trim syntax is the
%% same as the bb-syntax, with llx, lly, etc. defining the amount of
%% space to be trimmed away from the edge of the image (again in
%% points. 
%%
\usepackage{graphicx}
\usepackage{natbib}
\bibliographystyle{abbrvnat}  % or another style like abbrvnat, unsrtnat, etc.

\usepackage[labelfont=bf,size=small]{caption}
\usepackage{subcaption}
%%
%% The default LaTeX figure placement algorithm often causes large
%% amounts of white space and pages devoid of text. The commands below
%% tweak the parameters to something that is more reasonable and
%% should be sufficient to produce decent figure placement assuming
%% that the \begin{figure}...\end{figure} text is placed close to the
%% text where the figure should appear. When in doubt, use figure
%% placement parameters.
%%
\setlength\belowcaptionskip{0pt}
\setlength\abovecaptionskip{3pt}
\renewcommand{\textfraction}{0.05}
\renewcommand{\topfraction}{0.9}
\renewcommand{\bottomfraction}{0.65}
\renewcommand{\floatpagefraction}{0.95}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%%% Section heading format %%%%%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%
\makeatletter
\renewcommand{\section}{\@startsection%
{section}{1}{0mm}{-\baselineskip}%
{0.5\baselineskip}{\normalfont\large\bfseries}}%
\makeatother

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%%% Some useful abbreviations %%%%% 
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%
\newcommand{\ms}{\ensuremath{M_{\odot}}}
\newcommand{\rs}{\ensuremath{R_{\odot}}}
\newcommand{\ls}{\ensuremath{L_{\odot}}}
\newcommand{\kms}{\ensuremath{\mathrm{km}\,\mathrm{s}^{-1}}}
\newcommand{\fluxcgs}{\ensuremath{\mathrm{erg}\,\mathrm{s}^{-1}\,\mathrm{cm}^{-2}}}
\newcommand{\lumcgs}{\ensuremath{erg}\,\mathrm{s}^{-1}}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%%% Start of document %%%%% 
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\begin{document}
 
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%%% Title of proposal %%%%% 
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\begin{center}
\textbf{\large \textsc{\textsf{(MC)$^2$}}: The Magellanic Clouds Magellan Campaign}

Investigators: Jesse Han, Sergio Martin-Alvarez, Susan Clark
% Sophia Ridolfo, Scott Lucchini, Lisa Kewley
% Emma Chickles, Kevin Burdge
\end{center}

\section{Scientific Justification}

\textsc{\textsf{(MC)$^2$}} is a contiguous IFU survey of the central LMC with LLAMAS on Magellan, pursuing two goals: (i)~identify compact-object binaries via multi-epoch radial velocities (RV) of main-sequence turn-off and brighter stars, and (ii)~map the warm ionized ISM in Balmer and forbidden lines at \textbf{sub-parsec} resolution. In 2025B--2026A the team carried out a pilot of 8 nights covering a $\sim3'\times3'$ region centered on the LMC kinematic center at (RA, Dec) $=(80.90^\circ, -68.74^\circ)$ \citep{wan2020}. Our pipelines and analyses are mature: below we present \textbf{four concrete demonstrations from pilot data in hand} that motivate the 2026B request.

\noindent\textbf{1.~Confident re-detection of VFTS~243 validates absolute RV precision (Fig.~\ref{fig:vfts}).} Our footprint contains VFTS~243, the O-star + $\ge 9\,\ms$ black-hole binary of \citet{shenar2022}, in a 10.4-day orbit. Across \textbf{8 independent epochs} spanning 5 months we recover the published orbit blind (without fitting any orbital parameters) at $\chi^2=5.8$ (8 d.o.f.) using He\,{\sc ii}\,5411. The scatter about the Shenar et al.\ ephemeris is $\sim5\,\kms$, and is consistent across benches, channels, and nights---demonstrating $\lesssim5\,\kms$ absolute RV zero-point stability after our per-fiber barycentric and sky-line wavelength calibration. This is the precision required to detect and characterize new compact-object binaries with \textsc{\textsf{(MC)$^2$}}.

\noindent\textbf{2.~A population of RV variable candidates in hand, more epochs needed.} A cross-correlation analysis against a self-template identifies \textbf{tens} of RV-variable candidates across the pilot footprint, including main-sequence turn-off stars as faint as $r\sim19.6$ (example in Fig.~\ref{fig:msp}). Because VFTS~243 was prioritized in the pilot, most candidates currently have only 2--4 epochs---enough to flag variability but insufficient to fold orbits or reject systematics. \textbf{The 2026B epochs are the decisive step}: they raise the epoch count by $\sim50\%$ across the field, enabling robust period searches and orbital solutions for the candidate sample. SMASH photometry \citep{nidever2017} predicts $\gtrsim$18{,}000 usable stars in the full (MC)$^2$ footprint, compared with $\sim$900 stars monitored by VLT-FLAMES \citep{vlt_flames}.

\noindent\textbf{3.~Sub-parsec maps of the warm ionized ISM (Fig.~\ref{fig:ism}).} IFU spectroscopy allows continuum-subtracted, velocity-resolved emission-line maps that no narrowband imaging survey can produce. Figure~\ref{fig:ism} shows our H$\alpha$ map \textbf{side-by-side with DeMCELS} \citep{points2024}, the current state-of-the-art wide-field LMC H$\alpha$ map (DECam 4\,m, PSF $\sim1''$). Over the same field (MC)$^2$ resolves filamentary structure, bubble rims, and sharp ionization fronts that DeMCELS smooths away. In addition, we produce [O\,{\sc iii}]\,5007, [N\,{\sc ii}]\,6548/84, and [S\,{\sc ii}]\,6717/31 maps at matching resolution, together with a per-spaxel H$\alpha$ Gaussian centroid kinematic map with $\sim5\,\kms$ precision---an entirely new capability for this region of the LMC and the foundation for quantitative studies of ISM mixing and structure (led by Kado-Fong, Nowotka, and Clark).

\noindent\textbf{4.~Joint stellar-parameter + multi-epoch RV fitting with \texttt{MINESweeper} (Fig.~\ref{fig:msp}).} We have implemented a multi-epoch joint fitter built on \texttt{MINESweeper} \citep{cargile2020}: each star's $\log g$, $T_{\rm eff}$, $[\mathrm{Fe}/\mathrm{H}]$, and $[\alpha/\mathrm{Fe}]$ are held fixed across epochs while a per-epoch RV is fit jointly with broadband photometry and the LLAMAS spectrum. Leveraging the full stellar library and the shared SED boosts faint-star RV precision below what a blind single-epoch CCF can achieve. Figure~\ref{fig:msp} shows a test fit on a $r=19.6$ main-sequence turn-off candidate from the pilot. Demonstration 4 is the engine we will use to confirm the candidates of Demonstration 2.

\section{Stanford-led project}

\textsc{\textsf{(MC)$^2$}} is a Stanford-led effort. Stanford postdoc \textbf{Jesse Han} conceived the campaign, designed its observing strategy, and has executed essentially all of the analysis shown above (VFTS~243 recovery, CCF variable search, H$\alpha$/[O\,{\sc iii}]/[N\,{\sc ii}]/[S\,{\sc ii}] maps, Gaussian kinematics, \texttt{MINESweeper} multi-epoch fitting). Stanford PhD student \textbf{Viraj Manwadkar} developed the improved LLAMAS reduction pipeline---specifically the MISTY sky subtraction and per-bench flux calibration---whose impact is visible in Figure~\ref{fig:reduction}. These corrections are what make accurate absolute RVs and continuum-subtracted line maps possible. The remaining Stanford collaborators (Kado-Fong, Nowotka, Clark) lead the ISM analysis strand.

\section{Observing plan and time request}

We request \textbf{3 nights (2 dark, 1 gray) in 2026B} to extend the pilot with additional epochs. The added time will (a)~confirm the Demonstration-2 RV variable candidates by filling in their light curves to 5--7 epochs, enabling orbital solutions, (b)~contiguously tile the current footprint to the full $6'\times6'$ region centered on the LMC kinematic center, covering the $1\sigma$ kinematic-center uncertainty of \citet{wan2020}, and (c)~deepen the ISM maps to reach [S\,{\sc ii}] diagnostic S/N in the diffuse regions between filaments. Exposures are 10\,min per pointing; with a $30''$ step, a single epoch of the full field requires 24 exposure-hours (3 nights at 8 hr/night excluding overheads). Earlier LLAMAS RV work by Chickles \& Burdge (AM CVn He\,{\sc ii} campaign) with an identical setup corroborates the feasibility of this cadence.

\clearpage
%-----------------------------Figures Page--------------------------------

% Fig 1: Stanford pipeline - full width, landscape (2:1 aspect)
\begin{figure}[!t]
  \centering
  \includegraphics[width=0.90\textwidth]{reduction_demo.jpg}
  \caption{\textbf{Stanford-developed pipeline enables (MC)$^2$.}
  Pre (left) and post (right) MISTY sky-subtraction + per-bench flux-calibration
  on one pilot field. Raw extraction shows horizontal bench stripes; after
  Viraj Manwadkar's corrections the stellar field is photometrically flat.
  All science below relies on this pipeline.}
  \label{fig:reduction}
\end{figure}

% Fig 2: VFTS 243 RV - full width, very wide (3:1 aspect)
\begin{figure}[!t]
  \centering
  \includegraphics[width=0.98\textwidth]{vfts243_rv_ccf.png}
  \caption{\textbf{Demonstration 1: Confident re-detection of VFTS~243.}
  8 pilot epochs of the LMC BH binary VFTS~243 \citep{shenar2022}. Left: RV
  vs.\ orbital phase on the published ephemeris (\emph{no fit parameters
  adjusted}); $\chi^2 = 5.8$ (8 d.o.f.). Right: He\,{\sc ii}\,5411 spectra
  with CCF templates. Per-epoch RV precision $\sim5\,\kms$, with consistent
  absolute zero-point across benches and nights.}
  \label{fig:vfts}
\end{figure}

% Fig 3+4 side by side: DeMCELS/MC2 Ha (left) and MINESweeper (right)
\begin{figure}[!t]
  \centering
  \begin{subfigure}[t]{0.58\textwidth}
    \includegraphics[width=\textwidth]{demcels_vs_mc2.png}
    \caption{\textbf{Demo 3: sub-parsec ISM maps.} Same LMC field in H$\alpha$.
    \emph{Left:} DeMCELS narrow-band image (DECam 4\,m, \citealt{points2024}),
    PSF $\sim1''$ --- the prior best wide-field LMC H$\alpha$ map.
    \emph{Right:} LLAMAS-MC2 IFU at $0.3''$/spaxel, continuum-subtracted.
    LLAMAS also delivers [O\,{\sc iii}]/[N\,{\sc ii}]/[S\,{\sc ii}] maps and
    per-spaxel H$\alpha$ kinematics at $\sim5\,\kms$.}
    \label{fig:ism}
  \end{subfigure}
  \hfill
  \begin{subfigure}[t]{0.38\textwidth}
    \includegraphics[width=\textwidth]{test_dva_p1.jpg}
    \caption{\textbf{Demo 4: \texttt{MINESweeper} joint fit.}
    $r\sim19.6$ pilot star, 4 epochs. Stellar parameters
    $(\log g,\,T_{\rm eff},\,[\mathrm{Fe}/\mathrm{H}],\,[\alpha/\mathrm{Fe}])$
    are held fixed across epochs while a per-epoch RV is fit jointly with
    photometry and the LLAMAS spectrum --- the engine we will apply to the
    Demo-2 candidates in 2026B.}
    \label{fig:msp}
  \end{subfigure}
\end{figure}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%%% Additional page of references %%%%%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\clearpage
\bibliography{bib.bib}


%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%%% End of document %%%%%
%%%%%%%%%%%%%%%%%%%%%%%%%%%

\end{document}

